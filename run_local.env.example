# Copy to run_local.env and set values. run_local.env is gitignored.
# Brevo: Settings → SMTP & API → SMTP key (do not commit the real key).

MAIL_SMTP_HOST=smtp-relay.brevo.com
MAIL_SMTP_USER=your-login@smtp-brevo.com
MAIL_SMTP_PASSWORD=your-smtp-key
MAIL_FROM=your-verified-sender@example.com
